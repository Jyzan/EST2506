% !TEX program = xelatex
\documentclass[11pt,a4paper]{article}

% ============================================================
%  中文支持
% ============================================================
\usepackage[UTF8]{ctex}
\setmainfont{Times New Roman}
\setmonofont{Courier New}

% ============================================================
%  紧凑页面
% ============================================================
\usepackage{geometry}
\geometry{left=2cm,right=2cm,top=2cm,bottom=2cm,headheight=12pt}

\usepackage{fancyhdr}
\usepackage{lastpage}
\pagestyle{fancy}
\fancyhf{}
\fancyhead[L]{\small 智能联网时钟系统~~简介}
\fancyhead[R]{\small 江彦佐~~524442910013}
\fancyfoot[C]{\small 第~\thepage~页~/~共~\pageref{LastPage}~页}
\renewcommand{\headrulewidth}{0.4pt}
\fancypagestyle{plain}{%
  \fancyhf{}%
  \fancyfoot[C]{\small 第~\thepage~页~/~共~\pageref{LastPage}~页}%
  \renewcommand{\headrulewidth}{0pt}%
}

% ============================================================
%  基础包
% ============================================================
\usepackage{amsmath}
\usepackage{graphicx}
\usepackage{float}
\usepackage{array}
\usepackage{booktabs}
\usepackage{enumitem}
\setlist{nosep,leftmargin=2em}
\usepackage{caption}
\captionsetup{font=small,labelfont=bf,skip=4pt}

% ============================================================
%  紧凑代码（仅保留关键片段 ≤10行）
% ============================================================
\usepackage{listings}
\usepackage{xcolor}

\definecolor{codebg}{RGB}{248,248,248}
\definecolor{codeframe}{RGB}{200,200,200}
\definecolor{keywordcolor}{RGB}{0,0,200}
\definecolor{stringcolor}{RGB}{170,0,0}
\definecolor{commentcolor}{RGB}{0,128,0}
\definecolor{numbergray}{RGB}{140,140,140}

\lstset{
    basicstyle=\scriptsize\ttfamily,
    backgroundcolor=\color{codebg},
    frame=single,
    rulecolor=\color{codeframe},
    numbers=left,
    numberstyle=\tiny\color{numbergray},
    keywordstyle=\color{keywordcolor}\bfseries,
    stringstyle=\color{stringcolor},
    commentstyle=\color{commentcolor},
    showstringspaces=false,
    breaklines=true,
    tabsize=4,
    columns=flexible,
    keepspaces=true,
    xleftmargin=1.5em,
    framexleftmargin=1.5em,
    aboveskip=4pt,
    belowskip=4pt,
}
\lstdefinestyle{cstyle}{language=C}
\lstdefinestyle{pystyle}{language=Python}

% ============================================================
%  紧凑标题与段落
% ============================================================
\usepackage{titlesec}
\titleformat{\section}{\large\bfseries}{\thesection}{1em}{}
\titleformat{\subsection}{\normalsize\bfseries}{\thesubsection}{1em}{}
\titleformat{\subsubsection}{\normalsize\bfseries}{\thesubsubsection}{1em}{}
\titlespacing*{\section}{0pt}{12pt}{6pt}
\titlespacing*{\subsection}{0pt}{8pt}{4pt}
\titlespacing*{\subsubsection}{0pt}{6pt}{2pt}

\usepackage{indentfirst}
\setlength{\parindent}{2em}
\setlength{\parskip}{2pt}
\emergencystretch=2em

\usepackage{hyperref}
\hypersetup{colorlinks=true,linkcolor=black,urlcolor=blue,citecolor=blue}

\begin{document}

% ============================================================
%  封面
% ============================================================
\thispagestyle{empty}
\vspace*{2.5cm}

{\centering
{\Huge\bfseries 智能联网时钟系统}\\[0.6cm]
{\Large 嵌入式系统设计大作业简介}

\vspace{0.8cm}
\rule{0.6\textwidth}{0.5pt}

\vspace{2.0cm}

{\large
\begin{tabular}{rl}
\textbf{大作业名称：} & \underline{\makebox[6cm][c]{智能联网时钟系统}} \\[0.6cm]
\textbf{学生姓名：}   & \underline{\makebox[6cm][c]{江彦佐}} \\[0.6cm]
\textbf{学\hspace{2em}号：} & \underline{\makebox[6cm][c]{524442910013}} \\[0.6cm]
\textbf{专\hspace{2em}业：} & \underline{\makebox[6cm][c]{自动化}} \\[0.6cm]
\textbf{班\hspace{2em}级：} & \underline{\makebox[6cm][c]{自感2421}} \\[0.6cm]
\textbf{完成日期：}   & \underline{\makebox[6cm][c]{2026~年~6~月~15~日}} \\
\end{tabular}
}

\vfill
{\large 本简介为嵌入式系统与接口技术课程大作业提交材料}
\par}

\newpage

\tableofcontents
\thispagestyle{plain}
\newpage

% ============================================================
%  1. 系统概述与架构
% ============================================================
\section{系统概述与整体架构}

\subsection{系统简介}

本系统基于 S800硬件板卡 TI TM4C1294 作为下位机，Python 3.11 + PyQt5 作为 PC 上位机，通过 USB 虚拟串口实现 1:1 数字孪生镜像的双向协同。

四层架构分工：
\begin{itemize}
  \item 硬件外设层：S800 板卡 + I2C 7SEG 数码管 + I2C 按键 + GPIO USER1/USER2 + PCA9557 LED + 蜂鸣器，完成时钟走时、显示刷新、按键检测、闹钟触发等实时任务
  \item 串口协议层：UART0 中断驱动收发 + ring buffer 行拼装 + Token\_Matches() 容错解析，实现命令容错识别、多参数组合提取及 FORMAT RIGHT 逆序响应
  \item PC 后台通信层：SerialWorker QThread + NetworkWorker QThread，负责串口收发和 NTP、天气数据的异步请求
  \item PC UI 渲染层：PyQt5 MainWindow + TwinPanel + QPlainTextEdit 日志，完成数字孪生渲染、控制面板交互、四色日志显示和数据看板
\end{itemize}

\subsection{系统架构}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.7\textwidth]{架构图.png}
    \caption{系统整体架构框图}
    \label{fig:arch}
\end{figure}

数据流四主线：MCU 物理按键 → *EVT:KEY → PC 镜像面板脉冲高亮；PC 虚拟按键 → *SET:KEY → MCU Handle\_Key() 执行动作；PC 控制面板 → *SET:DATE/TIME... → MCU Process\_Command() 接收；PC 1Hz PING → *PONG 应答 → 延迟在线状态维护。PC 端 handle\_protocol\_line() 串联四条主线，统一分发到 twin.update\_digits/leds()、add\_log() 四色着色、update\_status() 状态栏刷新。

% ============================================================
%  2. 协议规范与容错
% ============================================================
\section{协议规范与容错设计}

\subsection{通信参数}

\begin{itemize}
  \item 物理层：USB 虚拟串口
  \item 波特率：115200
  \item 数据格式：8 数据位，无校验，1 停止位，无流控
  \item 字符编码：ASCII
  \item 行结束符：CR LF 或 LF 兼容
  \item 单帧最大长度：64 字节
\end{itemize}

\subsection{容错三件套}

\textbf{大小写不敏感}：MCU Process\_Command() 收到一行后先调用 Upper\_Copy()（main.c:2041）全转大写，故 *SET:TIME 与 *set:time 完全等价。

\textbf{空格容错}：按空格和 Tab 分割参数（main.c:2126），任意数量的空格或 Tab 均可正确分割。

\textbf{缩写识别}：Token\_Matches()（main.c:2052）用模式串中大写字母为必输前缀、小写字母可省略的策略。例如 MINute 最少须输入 3 字符 MIN，可写 MINU、MINUT、MINUTE；MI 仅 2 字符拒绝；全大写词如 ON、OFF 必须完整输入。

Upper\_Copy 与 Token\_Matches 实现（main.c:2041-2063）：
\begin{lstlisting}[style=cstyle]
static void Upper_Copy(char *dst, const char *src, uint16_t max) {
    uint16_t i;
    for (i = 0; i < max - 1 && src[i]; i++)
        dst[i] = (char)toupper((unsigned char)src[i]);
    dst[i] = '\0';
}

static bool Token_Matches(const char *token, const char *pattern) {
    // token 长度必须在[必输字符数, 完整pattern长度]之间且为前缀
    uint8_t req = 0, i;
    for (i = 0; pattern[i]; i++)
        if (isupper((unsigned char)pattern[i])) req++;
    if (strlen(token) < req || strlen(token) > strlen(pattern)) return false;
    return strncmp(pattern_upper, token_upper, strlen(token)) == 0;
}
\end{lstlisting}

\subsection{FORMAT RIGHT 逆序}

Send\_OK\_Value()（main.c:2096）在 g\_format 为 FMT\_RIGHT 时逐字符逆序后再发送 OK 应答：当前时间 12:30:45，LEFT 应答 OK 12.30.45，RIGHT 应答 OK 54.03.21。PC 端 parse\_disp\_event()（protocol.py:15）和 handle\_ok()（main.py:904）负责解析逆序报文并还原。

% ============================================================
%  3. 关键代码片段
% ============================================================
\section{关键代码片段与实现}

\subsection{MCU 中断架构}

SysTick 1ms 时基驱动四个分频标志（main.c:275），Timer0A ISR 每 1ms 刷新一位数码管并分频采样按键（main.c:292），UART ISR 最高优先级处理字节接收（main.c:320）。所有 I2C 访问集中在 Timer0A ISR 内消除总线竞争，LED 用 g\_led\_dirty 标志延迟写入避免阻塞。

UART 两级缓冲 ： g\_rx\_asm[ ] 行组装缓冲 → g\_rx\_ring[ ] 环形队列，行结束符到达时整行原子提交，主循环通过 UART\_GetLine() 取行调用 Process\_Command()。

UART RX ISR 行组装逻辑（main.c:354）：
\begin{lstlisting}[style=cstyle]
static void UART_RxIsrHandler(uint8_t byte) {
    if (byte == '\r' || byte == '\n') {
        if (g_rx_asm_len == 0) return;  // 忽略空行
        Mark_Serial_Activity();
        // 整行原子提交到环形缓冲并递增行计数
        for (i = 0; i < g_rx_asm_len; i++)
            g_rx_ring[g_rx_head++] = g_rx_asm[i];
        g_rx_ring[g_rx_head++] = '\n';
        rx_line_ready++;
        g_rx_asm_len = 0; return;
    }
    if (g_rx_asm_len >= LINE_MAX) {
        g_rx_asm_ovf = true; return;  // 超长行丢弃 设标志后续回传 ERROR LEN
    }
    g_rx_asm[g_rx_asm_len++] = (char)byte;
}
\end{lstlisting}

\subsection{PC 串口后台线程}

QThread（serial\_worker.py:10）将串口 I/O 从 Qt 主线程剥离，四个 pyqtSignal 跨线程通信：

\begin{lstlisting}[style=pystyle]
class SerialWorker(QThread):
    line_received = pyqtSignal(str)       # 收到一行 → 主线程协议解析
    connection_changed = pyqtSignal(bool) # 连接状态变化 → UI 指示灯
    latency_updated = pyqtSignal(int)     # 心跳延迟 ms
    error = pyqtSignal(str)               # 错误 → 弹窗+红色日志
\end{lstlisting}

以 timeout=0.1 打开串口，每轮先发送队列再 read 非阻塞收字节，遇换行符跨线程发射 line\_received。

\subsection{数字孪生面板}

\textbf{7SEG 自绘}：SevenSegmentDigit paintEvent（twin\_panel.py:107）中每个段用 QPainter drawRoundedRect，亮色 \#FF3030、暗色 \#220000、背景 \#050505，38 字符映射表与 MCU 段码完全一致。

\textbf{双向同步}：下行 PC 虚拟按键通过 *SET:KEY NAME 下发（main.py:796），上行 *EVT:KEY NAME 则 pulse\_key() 按钮高亮 200ms（twin\_panel.py:242）。MCU 按下 USER1 时 PC 自动触发 NTP 对时流程。

\textbf{虚拟按键长按}：HoldButton（twin\_panel.py:9）覆写 mousePressEvent/mouseReleaseEvent，按下启 800ms 定时器，到期触发长按信号无需松手；松手时若未触发长按则抛短按信号。

\begin{lstlisting}[style=pystyle]
def handle_protocol_line(self, line: str):
    if line.startswith("*PONG"):          # 心跳 更新延迟
    if line.startswith("*EVT:DISP"):      # 数码管同步
    if line.startswith("*EVT:LED"):       # LED 同步
    if line.startswith("*EVT:KEY"):       # 按键脉冲 + USER1→NTP
    if line.startswith("*EVT:ALARM"):     # 闹钟弹窗
    if line.startswith("*EVT:MODE"):      # 昼夜模式同步
    if line.startswith("OK"):             # GET 应答解析
\end{lstlisting}

\subsection{MCU 显示与编辑}

Build\_Display()（main.c:1026）按优先级组装 disp\_buf：倒计时优先 > 天气短显 > 流水消息 > 正常时钟，NIGHT 模式仅显示时分 4 位。编辑 FSM 由 FUNC 键驱动 IDLE→EDIT\_DATE→EDIT\_TIME  →EDIT\_ALARM→IDLE 四态循环，SHIFT 切换高亮字段、ADD 循环加 1 含进位钳制、SAVE 保存退出、5 秒超时放弃退出。

Display\_Refresh() 防鬼影与闪烁处理（main.c:892）：
\begin{lstlisting}[style=cstyle]
// 先清零位选消除鬼影 再通过自动递增突发写入段码和新位选
I2C0_WriteByte(TCA6424_I2CADDR, TCA6424_OUTPUT_PORT2, 0x00);
blink_off = (disp_blink_mask & (1U << g_scan_pos)) && !g_blink_on;
code = blink_off ? 0U : SegCode(disp_buf[g_scan_pos]);
if (!blink_off && disp_dp[g_scan_pos]) code |= 0x80;
I2C0_WriteTwo(TCA6424_I2CADDR,
    TCA6424_OUTPUT_PORT1 | TCA6424_AUTO_INC,
    code, (1U << g_scan_pos));
\end{lstlisting}

% ============================================================
%  4. 扩展功能
% ============================================================
\section{扩展功能实现}

\subsection{E1 NTP 网络对时（2分）}

实现：ntp\_helper.py 三服务器容错（ntp.aliyun.com / cn.ntp.org.cn / ntsc.ac.cn），NetworkWorker QThread 异步执行不阻塞 UI。PC 点击按钮或 MCU 按 USER1（*EVT:KEY USER1→PC 自动触发）均走 ntp\_sync() 流程。完成后下发 *SET:DATE + *SET:TIME + \texttt{*NTP SYNC} 三连命令，MCU LED7 常亮指示已同步，超 24 小时未同步慢闪。\texttt{*NTP SYNC} 为 E1 扩展协议，用于在日期和时间写入成功后标记同步状态。

\subsection{E2 天气获取（3分）}

实现：weather\_helper.py 从 wttr.in 免 Key 拉取，六种天气码（SUN/CLD/OVC/RAI/SNO/FOG）映射，通过 \texttt{*SET:WEA <T> <COND>} 下发到 MCU；其中温度范围为 -40 至 50，天气码为 SUN/CLD/OVC/RAI/SNO/FOG。MCU LED4 晴常亮、LED5 雨雪慢闪、LED6 高温≥30°C 常亮。USER2 短按短显天气 5 秒，期间 *EVT:DISP 正常上报 PC 镜像跟随。启动后 1 秒首次拉取，此后 30 分钟自动刷新。

\subsection{E3 自动昼夜模式（2分）}

实现：astral\_helper.py 按上海经纬度（31.23°N, 121.47°E）通过 astral 库计算日出日落，手动模式从 MCU 取当前时/分代入判定。DAY 完整 8 位显示 + 全 LED 启用；NIGHT 仅显时/分 4 位 + 仅心跳 LED + 蜂鸣器抑制。auto\_daynight() 每分钟周期检查，仅模式跨越时下发避免重发。

\subsection{E4 数据可视化（1分）}

实现：log\_store.py 将事件持久化到 events.csv（时间戳/类型/数据），chart\_widget.py 用 matplotlib FigureCanvasQTAgg 嵌入 Qt，三张子图：闹钟触发时间按日期折线、NTP 误差柱状图、按键热度横向条形图。数据为空时友好提示 "No data"。

% ============================================================
%  5. 自主增加功能：情景倒计时（§4.3，8分）
% ============================================================
\section{自主增加功能：情景倒计时系统}

\subsection{动机}

传统闹钟基于绝对时刻触发，不适应演示、专注训练、短时提醒等需要相对时长的场景。本功能与基础闹钟互补：闹钟按绝对时间、倒计时按相对时长，二者独立并行。创新在于板上按键可独立编辑运行、三情景收尾、PC 进度环 + 语音联动。

\subsection{设计}

五态 FSM：IDLE → EDIT → RUN ↔ PAUSE → DONE → IDLE。EXT 键短按进入编辑/启动/暂停/继续，长按停止。编辑态 SHIFT 分秒字段切换、ADD 加 1、SAVE 减 1。三种情景由 g\_cd\_scene 选择：0 滚动文本（到点流水 g\_cd\_msg）、1 闪烁庆祝（DONE 全屏快闪）、2 静默（仅显 --00--）。

协议扩展 *SET:COUNTDOWN TIME/SCENE/MSG/START/PAUSE/RESUME/STOP。

\begin{itemize}
  \item \texttt{*EVT:CD STATE <state> <remain> <total> <scene>} 1Hz 心跳上报，PC 据此同步进度环与状态标签。state 取值 IDLE / EDIT / RUN / PAUSE / DONE，remain 和 total 为剩余秒与总秒，scene 取值 0 滚动文本、1 闪烁庆祝、2 静默。
  \item \texttt{*EVT:CD DONE} 归零瞬间单次上报，PC 据此触发语音播报到点文本并停定时器。
\end{itemize}

同时支持 \texttt{*GET:COUNTDOWN} 查询当前倒计时状态。PC 端 CountdownRing 圆弧进度环 + TTS 语音倒数 1-5 秒。RUN/PAUSE/DONE 状态通过数码管、PC 进度环和语音反馈呈现，不接管 8 位 LED，避免覆盖系统心跳、闹钟、编辑和串口收发等基础指示。完全复用 MCU 既有蜂鸣器、显示和串口机制，不新增依赖。

\subsection{实现}

MCU 状态枚举与全局变量定义了 CD\_IDLE/EDIT/RUN/PAUSE/DONE 五态，g\_cd\_total\_s 1-3599 可设定时长，g\_cd\_scene 0/1/2 选情景，g\_cd\_msg 存滚动文本。

核心按键处理 Countdown\_HandleKey()（main.c:1800）：
\begin{lstlisting}[style=cstyle]
static bool Countdown_HandleKey(key_code_t key) {
    if (g_cd_state == CD_IDLE) {
        if (key == KEY_EXT && g_edit_state == ST_IDLE) {
            g_cd_state = CD_EDIT; g_cd_field = 1; Send_CD_Event();
            return true;
        }}
    switch (g_cd_state) {
    case CD_EDIT:
        if (key == KEY_SHIFT) g_cd_field = (g_cd_field+1)%2; // 分秒切换
        if (key == KEY_ADD)   /* 当前字段+1 */;
        if (key == KEY_SAVE)  /* 当前字段-1 */;
        if (key == KEY_EXT)   Countdown_Start(); // 确认启动
    case CD_RUN:  if (key == KEY_EXT) {PAUSE; Send_CD_Event();}
    case CD_PAUSE: if (key == KEY_EXT) {RUN;   Send_CD_Event();}
    case CD_DONE:  if (key == KEY_EXT||key == KEY_FUNC) Countdown_Stop();
    }
}
\end{lstlisting}

Build\_Display() 中倒计时优先级最高：EDIT 显示 CdMMSS 闪烁高亮字段，RUN/PAUSE 显示 Cd 加剩余秒数，DONE 按情景设置滚动文本/全 DONE 闪烁/--00--，均设 10 秒超时兜底。Time\_Tick\_1s() 中 RUN 态递减 g\_cd\_remain\_s，归零转 DONE 并上报 *EVT:CD DONE。

协议命令族 Countdown\_HandleCommand()（main.c:1856）支持 TIME/SCENE/MSG/START/PAUSE/RESUME/STOP 七种子命令，以及向后兼容的 *SET:COUNTDOWN 数字直设时长。

PC 端 CountdownRing（twin\_panel.py:258）为 QWidget 自绘，drawArc 按 remain/total 比例渲染圆弧，中心显示剩余分秒和状态标签。TtsSpeaker（main.py:65）封装 pyttsx3 和 win32com SAPI 双通道，倒计时最后 5 秒语音播报，DONE 播报到点文本。UI 提供分秒 SpinBox、情景下拉框、文本输入、开始/暂停/停止按钮和语音复选框。

\subsection{演示}

\begin{enumerate}
  \item 在UI界面或板子上点击EXT开始编辑，设置时长，如10 秒，再次点击开始
  \item 数码管显示 Cd0010，PC 进度环按剩余比例同步收缩，8 位 LED 保持原有系统状态指示
  \item 最后 5 秒语音倒数（可选），数码管与 PC 进度环每秒递减，LED 不参与倒计时进度显示
  \item 归零后：情景 0 流水文本 + 蜂鸣器节奏 10 秒；情景 1 全屏 DONE 闪烁 + 蜂鸣快闪；情景 2 静显--00-- ，10 秒无操作自动退出
  \item 运行中可 EXT 暂停/继续，长按 EXT 停止；闹钟功能独立可用
\end{enumerate}

% ============================================================
%  6. 难点解决
% ============================================================
\section{难点解决}

\subsection{心跳刷屏}

1Hz 的 *PONG、*EVT:DISP、*EVT:LED 每秒追加 3 行日志，运行数分钟导致卡顿。

解决：setMaximumBlockCount(1000) 限制最大行数（main.py:615）；is\_heartbeat() 过滤心跳包（protocol.py:8）；「显示心跳」复选框（默认关闭）过滤后日志区仅显示闹钟、按键、ERROR 等事件。

\subsection{串口断连与自动重连}

拔插 USB 或心跳超时后，旧 SerialWorker 可能仍有 queued signal 尚未送达；若直接创建新线程，可能出现旧信号误更新 UI 或重复处理协议行。

解决：心跳 3s 超时后自动 close 并延迟重连；创建新 worker 前统一 disconnect 旧信号并 deleteLater；回调入口检查 sender 是否为当前 worker，晚到旧信号直接丢弃。

\subsection{显示与蜂鸣资源抢占}

时钟、消息、天气、倒计时、闹钟都会争用 8 位数码管和蜂鸣器。若简单覆盖状态，容易出现倒计时结束后不恢复时钟、远程 BEEP 打断闹钟、夜间模式仍响铃等问题。

解决：Build\_Display() 固定优先级为倒计时 > 天气短显 > 消息 > 时钟；BEEP 使用独立计时状态，不覆盖闹钟 ringing；夜间模式统一抑制闹钟、倒计时和远程蜂鸣，只保留状态上报。

% ============================================================
%  7. 补充说明
% ============================================================
\section{补充说明}
\begin{itemize}
  \item \textbf{数字孪生镜像面板按键}: 《MCU与PC端开发要求》中第23条要求图片中只出现了8个按键FUNC、SHIFT、ADD、SAVE、USR1、USR2、K7、K8，但在《大作业题目》4.1 P3中明确要求实现八位按键+USR1+USR2，因此UI界面实际显示按键增加了DISP、SPEED，与原来六个按键2行×4列排列; 下方USR1、USR2以蓝色调独立样式水平排列, 实现视觉区分。
  \item \textbf{昼夜模式时基}: 自动昼夜的定时检查用板子上的时间; 手动「应用昼夜」按钮先向 MCU 查询当前钟表时间(*GET:TIME 返回 HH.MM.SS), 再由 current\_daynight 代入计算。
  \item \textbf{ERROR LEN 提示}：《MCU与PC端开发要求》中要求行长超过 64 后提示 ERROR LINE TOO LONG，但《大作业题目》5.2要求ERROR只能为SYNTAX/PARAM/RANGE/LEN/BUSY其中之一，故实现上提示ERROR LEN。
  \item \textbf{容错规则验证}: 协议演示页提供缩写演示按钮(如 DISP 代替 DISPlay、MIN 代替 MINute)和大小写混合演示按钮(如 *sEt:FoRmAt rIgHt), 可在不连接 MCU 的情况下直观理解容错的运作机制。
  \item \textbf{SAVE按键功能}：在自主情景倒计时扩展功能触发时，为了方便设置相对时间，引入了SAVE按键作为控制减一的编辑键，当倒计时结束之后，SAVE按键恢复成和文档要求一样的功能。
  \item \textbf{NIGHT编辑状态}：文档中要求NIGHT模式下只显示前四位，又要求编辑状态下要能够显示编辑的数字，在实现中NIGHT模式下编辑也只显示前四位。
  \item \textbf{SET:KEY USER1说明}：文档要求虚拟按键和物理按键一样的功能，同时要求长按USER1按键时不触发对时，因此在UI界面需要对长按和短按进行区分，由于MCU端无法将一个命令解析为对时和显示x.SY.xx两种行为，且短按USER1已经发送了EDIT TIME等指令，因此只有在UI界面长按USER1才会发送SET:KEY USER1，否则在UI界面长按USER1按键将不会出现x.SY.xx。 
  
\end{itemize}

\vfill
\begin{center}
% \rule{12cm}{0.5pt}

{\small 智能联网时钟系统~~·~~嵌入式系统设计~~·~~大作业简介}\\[2pt]
\end{center}

\end{document}
